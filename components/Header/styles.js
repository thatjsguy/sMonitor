import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  header: {
    color: '#FFFFFF',
    backgroundColor: '#000000',
    padding: 20,
    fontSize: 20,
    textAlign: 'center',
  }
});
